package org.gembaboo.binarysearch;

public class BinarySearch {

    // Finds the position of an element in an ordered array
    public int getPosition(int element, int[] array) {

        if (null == array) {
            throw new IllegalArgumentException("array can not be null");
        }

        return -1;


    }
}
